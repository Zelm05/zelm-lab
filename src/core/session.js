/* ==========================================================================
 * src/core/session.js —— 登录态快照
 *
 * 原站用 window.__zelmUser 在各脚本之间传递「当前登录用户」：
 *   写入方：home.head1.js（拉 /api/me 后写入）
 *   读取方：community.js（判断游客 / 已登录，决定是否弹登录框）
 *
 * 现在改为显式模块：写入方是 Pinia 的 user store，读取方是社区模块。
 * 有意用「普通对象」而不是 ref —— 原站脚本读的是同步快照，不是响应式值；
 * 组件需要响应式时用 useUserStore()。
 * ========================================================================== */
export const session = {
  user: undefined,
};
